name: VIRTUOS INTEGRATION 

on: 
  push: 
    branches: [main]
  pull_request:
        branches: [main]
  workflow_dispatch:

jobs:
    open-git-container:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout Repo to $GITHUB_WORKSPACE
          run: ls -la
          
  

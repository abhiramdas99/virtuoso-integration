name: VIRTUOS INTEGRATION 

on: 
  push: 
    branches: [main]
  pull_request:
        branches: [main]
  workflow_dispatch:

jobs:
    open-git-container:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout Repo to ${{env.GITHUB_WORKSPACE}}
          run: ls -la
          
  
